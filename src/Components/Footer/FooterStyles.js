import styled from "styled-components"
import { COLORS, FONTS, IMAGES, DEVICE, SIZE } from "../../Constants/Themes";


export const Wrapper = styled.section`
  .iSIFGq {
    margin: 0;
  }

  .contact-short {
    max-width: 60vw;
    margin: auto;
    padding: 5rem 10rem;
    background-color: ${COLORS.bg};
    border-radius: 1rem;
    box-shadow: ${COLORS.shadowSupport};
    transform: translateY(50%);

    .grid div:last-child {
      justify-self: end;
      align-self: center;
    }
  }

  footer {
    padding: 14rem 0 9rem 0;
    background-color: ${COLORS.footer_bg};
    h3 {
      color: ${COLORS.hr};
      margin-bottom: 2.4rem;
    }
    p {
      color: ${COLORS.white};
    }
    .footer-social--icons {
      display: flex;
      gap: 2rem;

      div {
        padding: 1rem;
        border-radius: 50%;
        border: 2px solid ${COLORS.white};

        .icons {
          color: ${COLORS.white};
          font-size: 2.4rem;
          position: relative;
          cursor: pointer;
        }
      }
    }
  }

  .footer-bottom--section {
    padding-top: 9rem;

    hr {
      margin-bottom: 2rem;
      color: ${COLORS.hr};
      height: 0.1px;
    }
  }

  @media (max-width: ${DEVICE.mobile}) {
    .contact-short {
      max-width: 80vw;
      margin: 4.8rem auto;
      transform: translateY(0%);
      text-align: center;

      .grid div:last-child {
        justify-self: center;
      }
    }

    footer {
      padding: 9rem 0 9rem 0;
    }

    .footer-bottom--section {
      padding-top: 4.8rem;
    }
  }
`;